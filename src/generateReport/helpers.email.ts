// import type { DAOData } from "./types.common";

// async function generateReportHTML(daoData: DAOData[], summary: any[]): Promise<string> {
//   const template = await fs.readFile("views/report.ejs", "utf-8");
//   return ejs.render(template, { daoData, summary });
// }
